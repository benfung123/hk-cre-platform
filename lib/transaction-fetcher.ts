// Stub: transaction-fetcher - provides transaction data queries
import { Transaction } from '@/types'

export async function getTransactionsByDistrict(_district: string): Promise<Transaction[]> {
  return []
}

export async function getTransactionByPropertyId(_propertyId: string): Promise<Transaction[]> {
  return []
}
